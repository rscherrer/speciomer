% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_loci_by_edge.R
\name{read_loci_by_edge}
\alias{read_loci_by_edge}
\title{Read locus-wise variables on a per-edge basis}
\usage{
read_loci_by_edge(root, variables)
}
\arguments{
\item{root}{Path to the simulation folder}

\item{variables}{What variables to read}
}
\value{
A tibble
}
\description{
Reads locus-specific data through time and rearranges the output tibble
with one row per edge instead of one row per locus.
}
\details{
Uses internally \code{read_loci} with \code{architecture = TRUE},
then reads the network architecture (the \code{edges} tibble returned by
\code{read_architecture}) to reformat.
}
\note{
Assumes that the genetic architecture file is present.
}
\examples{

root <- system.file("extdata", "sim-example", package = "speciomer")
read_loci_by_edge(root, "Fst")

}
\seealso{
\code{read_loci}, \code{read_architecture}, \code{get_locus_data_n}
}
